name: Deploy to Hostinger

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Trigger deploy webhook
        run: |
          curl -f "https://sip-phbs.online/deploy.php?token=${{ secrets.DEPLOY_SECRET }}"